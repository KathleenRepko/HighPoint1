/* eslint no-var:0 */
define(function (require) {
  var Dispatcher = require('flux').Dispatcher;

  // Create dispatcher
  var appDispatcher = new Dispatcher();

  // Add any extensions to our flux dispatcher here...

  return appDispatcher;
});
